# Tested dependency set.
#
# The versions in the trailing comments are the exact ones this dashboard was
# built and tested against (Python 3.11.15, Linux, CPU only, 2026-09-19).  The
# ranges are deliberately conservative rather than "latest of everything": the
# research notebook targets Python 3.12 / Qiskit 1.x-2.x and the framework code
# is unchanged, so anything inside these ranges is expected to behave the same.
#
# Python: 3.11 or 3.12.  (3.13 is untested here.)
# GPU: not required and not used.  All inference is CPU.
# TensorFlow: NOT required.  The forecaster falls back to the scikit-learn MLP.

numpy>=1.26,<3            # tested 2.4.4
pandas>=2.0               # tested 3.0.2
scipy>=1.11               # tested 1.17.1  (Clopper-Pearson QBER bound)
scikit-learn>=1.4         # tested 1.8.0
qiskit>=1.0,<3            # tested 2.5.2
qiskit-aer>=0.14          # tested 0.17.2
cryptography>=42          # tested 46.0.7  (AES-GCM)
streamlit>=1.36           # tested 1.64.0
plotly>=5.20              # tested 7.1.0
joblib>=1.3               # tested 1.5.3   (loading optional model artifacts)

# testing only
pytest>=8.0               # tested 9.1.1
